name: test workflow

on:
  pull_request:
  workflow_dispatch:

jobs:
  firstjob:
    runs-on: ubuntu-latest
    steps:
      - name: first step
        run: echo "first step executed"

      - name: second step
        run: echo "second step executed"

  secondjob:
    runs-on: ubuntu-latest
    needs: firstjob
    steps:
      - uses: actions/checkout@v4

      - name: fetch the repo
        run: |
          ls -a
          echo "repo downloaded"

      - name: fail if TODO exists
        run: |
          if grep -R "TODO" .; then
            echo "Found TODO. Failing build."
            
          else
            echo "No TODOs found."
          fi

      - name: end
        run: echo "end"
